var colors = [
  'white',
  'black',
  'custom'
];

console.log(colors[0]);

var itemThree;
itemThree = colors[2];
var numColors;
numColors = colors.length;

var todo = [
  'play',
  'study',
  'code'
];

todo.push('breathe');
todo.splice(2, 2);
console.log(todo);
